name: build
on: push
jobs:
    build:
        runs-on: ubuntu-20.04
        steps:
            -   uses: actions/setup-node@v3
                with:
                    node-version: 16
            -   run: ls -la
            -   run: npm install
            -   run: npm run build

